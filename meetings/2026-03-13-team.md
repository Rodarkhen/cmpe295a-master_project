Meeting Type: Team-Only
Date: 2026-03-13
Start–End Time: 4:00 PM – 5:30 PM
Location/Mode: Zoom

Attendees:
- Aruna Samhitha Mutyala
- Het Bhalala
- Parth Gala
- Rodrigo Chen

---

## 1) Accomplished Since Last Meeting

- Parth Gala: `RetailPricingEnv` gymnasium wrapper class implemented — `observation_space` (Box, shape=(4,), dtype=float32), `action_space` (Discrete, n=5), `reset()` returning normalized state vector, and `step()` computing the multi-objective reward; curriculum warm-up flag added (penalties inactive for first 50 episodes). HPC access still pending; running initial smoke tests on local CPU.
- Aruna Samhitha Mutyala: SHAP churn ensemble model scaffolding complete — scikit-learn pipeline with RandomForest + LogisticRegression stacking; Prophet forecasting baseline running on 30-day synthetic sales data for 5 SKUs; initial SHAP feature importance plots generated for churn model.
- Het Bhalala: ESP32 DevKit V1 and MAX7219 components received (March 10); single-display price update successfully tested via MicroPython + umqtt.simple — price `12.99` pushed over MQTT and rendered on 7-segment display within 800ms.
- Rodrigo Chen: Canonical product price record schema defined and shared in GitHub — JSON format with fields: `product_id`, `sku`, `base_price`, `competitor_price`, `inventory_level`, `timestamp`; FastAPI supplier ingestion endpoint scaffolded with schema validation via Pydantic.

---

## 2) Accomplished During This Meeting

- Sprint 1 (March 1–14) mid-sprint status review: team confirmed all four primary deliverables are on track; no sprint scope changes required.
- Parth Gala walked through the `RetailPricingEnv` implementation: showed the observation vector construction, reward function code (`profit - 0.3*volatility - 0.5*churn`), and the curriculum warm-up logic. Team reviewed and approved; Rodrigo Chen suggested adding a `render()` method to print a human-readable state summary for debugging — Parth will add before end of sprint.
- Het Bhalala demonstrated the single-display ESP32 + MAX7219 price update demo over Zoom screen share; team confirmed the hardware milestone is complete; agreed next step is daisy-chaining 4 displays and adding the MQTT topic routing logic (`retail/price/{product_id}`).
- Reviewed Rodrigo Chen's product price record JSON schema — team agreed on the schema; Parth Gala noted that `inventory_level` and `competitor_price` fields map directly to the RL environment's state vector, confirming integration compatibility.
- Discussed Sprint 2 scope (March 15–28): agreed on DQN baseline hyperparameters — learning rate 1e-3, epsilon start 1.0 decaying to 0.05 over 500 episodes, replay buffer size 50,000, batch size 64, target network update every 100 steps; Parth will use Stable-Baselines3 DQN implementation rather than custom to save time and validate against a reference implementation.
- Discussed simulated retail dataset format for Sprint 2 training: agreed on a 30-day synthetic dataset with 10 SKUs, daily demand sampled from a Poisson distribution, competitor prices from a random walk, and inventory replenishment every 7 days — Aruna Samhitha Mutyala will generate this dataset using her Prophet baseline code.

---

## 3) Issues / Blockers

- Parth Gala: SJSU HPC cluster GPU access still not provisioned (14 days since application); DQN training on local CPU is possible for small episode counts but will be the primary bottleneck for Sprint 2 if HPC access is not granted before March 15.
- Rodrigo Chen: FastAPI supplier ingestion endpoint does not yet have a database backend; product price records are being held in memory only — PostgreSQL integration is needed before the system can persist supplier data across restarts.
- Het Bhalala: 4-display daisy-chain test not yet started; will begin after Sprint 1 closes on March 14.

---

## 4) Action Items

- Action: Add `render()` method to `RetailPricingEnv` for human-readable state/reward debug output | Owner: Parth Gala | Due: Mar 14
- Action: Run initial DQN training with SB3 on local CPU — target 200 episodes with SB3 DQN defaults; verify reward is trending in correct direction | Owner: Parth Gala | Due: Mar 14
- Action: Generate 30-day synthetic retail dataset (10 SKUs, Poisson demand, random-walk competitor prices, weekly replenishment) for use as DQN training environment seed | Owner: Aruna Samhitha Mutyala | Due: Mar 17
- Action: Begin 4-display MAX7219 daisy-chain wiring and MQTT topic routing (`retail/price/{product_id}`) on ESP32 | Owner: Het Bhalala | Due: Mar 20
- Action: Add PostgreSQL backend to FastAPI supplier ingestion service; use SQLAlchemy ORM with Alembic migrations | Owner: Rodrigo Chen | Due: Mar 20
- Action: Check in with Prof. Bhaskar via email on HPC account status | Owner: Parth Gala | Due: Mar 14

---

## 5) Plans / Goals for Next Meeting

- Sprint 1 final deliverables confirmed and committed to GitHub before March 14 end of day.
- March 20 advisor meeting: present Sprint 1 completions to Prof. Bhaskar; begin Sprint 2 planning with advisor input.
- Parth Gala: restart DQN training on HPC GPU (if access granted) or continue on CPU with reduced episode budget; target 500–1000 training episodes by March 20.
- Team to coordinate on shared GitHub branch strategy for Sprint 2 to avoid merge conflicts as multiple components begin to converge.
