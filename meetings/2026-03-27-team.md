Meeting Type: Team-Only
Date: 2026-03-27
Start–End Time: 4:00 PM – 5:45 PM
Location/Mode: Zoom

Attendees:
- Aruna Samhitha Mutyala
- Het Bhalala
- Parth Gala
- Rodrigo Chen

---

## 1) Accomplished Since Last Meeting

- Parth Gala: Full DQN training job submitted on SJSU HPC — 1000-episode run completed (12-hour wall time); reward curve logged to CSV and plotted; convergence observed around episode 700 with terminal avg reward ~+22% vs. rule-based fixed markup baseline on same 30-day synthetic dataset; price change frequency reduced by 38% compared to baseline (volatility penalty effective). Rule-based baseline implemented and measured: avg daily profit = $142.30 (reference for comparison).
- Parth Gala: Parameter sweep completed — 9 runs of 500 episodes each across λ ∈ {0.1, 0.3, 0.5} and γ ∈ {0.3, 0.5, 0.7}; best pair identified as λ=0.3, γ=0.5 (matching initial design choice) with λ=0.3, γ=0.3 as a close second; confirms original parameter selection was empirically sound.
- Aruna Samhitha Mutyala: Prophet model retrained with promotions flag and US federal holiday calendar; MAPE improved from 8.3% to 4.7% on hold-out week; SHAP ensemble churn model v1 finalized — top 5 features by SHAP value: price_change_magnitude, days_since_last_purchase, competitor_price_delta, day_of_week, inventory_level.
- Het Bhalala: 4-display daisy-chain demo fully working — all 4 MAX7219 displays updating in sequence via MQTT topic routing; added NVS fallback (last-known price stored in ESP32 flash) per NFR-02; price push latency confirmed at <1.2s for all 4 displays.
- Rodrigo Chen: PostgreSQL instance deployed to shared Docker environment (accessible to all team members via VPN); FastAPI supplier ingestion service connected to shared DB; integration test with Parth Gala's RL backend environment confirmed — `inventory_level` and `competitor_price` pulled from DB and inserted into state vector correctly.

---

## 2) Accomplished During This Meeting

- Parth Gala presented DQN training results: shared reward curve plot showing convergence at episode ~700; explained the +22% profit improvement over the rule-based baseline; team reviewed the parameter sweep heatmap and confirmed λ=0.3, γ=0.5 as the final production values.
- Team celebrated a significant integration milestone: Rodrigo Chen's supplier ingestion service successfully feeds live `inventory_level` and `competitor_price` values into Parth Gala's `RetailPricingEnv` state vector — this is the first end-to-end data flow from the ingestion layer to the RL backend.
- Discussed Sprint 3 scope (March 29–April 11): agreed on two primary goals for Parth — (1) implement DDPG actor-critic network for continuous price action space, replacing the discrete 5-step DQN action space; (2) run comparative hyperparameter tuning between DQN and DDPG using the same 30-day synthetic dataset and report terminal reward for both.
- Reviewed Aruna Samhitha Mutyala's SHAP churn feature importance findings — team agreed that `price_change_magnitude` as the top SHAP feature validates the volatility penalty design in the reward function; this result should be highlighted in the paper.
- Discussed the paper outline structure for the IEEE ICCE 2026 submission: agreed on section order — Abstract, Introduction, Related Work, System Architecture, RL Pricing Agent (Parth), XAI Transparency Layer (Aruna), Edge IoT Implementation (Het), Data Ingestion & Orchestration (Rodrigo), Experimental Evaluation, Discussion, Conclusion. Paper writing begins in Sprint 9 (295B).
- Identified need for a results tracking spreadsheet: Rodrigo Chen to create a shared Google Sheet with columns for metric, value, component, sprint, and notes; all members to log quantitative results going forward.

---

## 3) Issues / Blockers

- Parth Gala: DDPG implementation for Sprint 3 will require a continuous action space change in `RetailPricingEnv` — the current `Discrete(5)` action space must be replaced with a `Box` action space (price delta ∈ [−$1.00, +$1.00]); this is a breaking change that will require coordinating with Rodrigo Chen to ensure the DB-to-state pipeline is unaffected.
- Aruna Samhitha Mutyala: SHAP values are currently computed offline (post-hoc on stored predictions); real-time SHAP inference on each pricing decision is needed for the transparency API, which adds latency — need to profile SHAP computation time per prediction.
- Het Bhalala: 4-display demo uses a static product-to-display mapping hardcoded in MicroPython; a dynamic configuration mechanism is needed before the system can scale to more displays without reflashing firmware.

---

## 4) Action Items

- Action: Refactor `RetailPricingEnv` to support continuous action space (`Box(low=-1.0, high=1.0, shape=(1,))` for price delta); verify reward function is unchanged | Owner: Parth Gala | Due: Apr 3
- Action: Implement DDPG actor-critic network using SB3 DDPG; begin training on 30-day synthetic dataset with same hyperparameter baseline as DQN | Owner: Parth Gala | Due: Apr 7
- Action: Profile SHAP computation time per prediction in isolation; target <100ms per inference for real-time use | Owner: Aruna Samhitha Mutyala | Due: Apr 3
- Action: Create shared results tracking Google Sheet; seed with all quantitative results logged so far (DQN reward, Prophet MAPE, display latency, churn SHAP) | Owner: Rodrigo Chen | Due: Mar 31
- Action: Design dynamic product-to-display configuration mechanism for ESP32 — implement as an MQTT-configurable JSON mapping stored in NVS | Owner: Het Bhalala | Due: Apr 7

---

## 5) Plans / Goals for Next Meeting

- April 3 advisor meeting: present Sprint 2 final results to Prof. Bhaskar — DQN reward curve, parameter sweep heatmap, baseline comparison (+22% profit); share results tracking spreadsheet for advisor review.
- Parth Gala to report Sprint 3 early progress on DDPG implementation and continuous action space refactor.
- Begin discussing Sprint 4 scope (April 12–25): MQTT integration between the RL pricing agent and the ESP32 edge nodes for a full end-to-end price update flow.
